Resource File Unit
------------------

This directory contains zip files containing Resource File Unit releases from
the present release back to v1.0.1.

The zip files are all of the form dd-resfile-x.x.x.zip where x.x.x is the
version number of the release.

Unless you have good reasons not to, you should always download the latest
version.